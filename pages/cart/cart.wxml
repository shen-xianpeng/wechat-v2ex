<view class="entry" wx:for="{{groupList}}" wx:for-index="j" wx:key="id">

  <view id="{{j}}" class="entry-header {{               activeHoverIndex==j?'hover-element': '' }}" bindtouchstart="touchStartElement" bindtouchmove="touchMoveElement" bindtouchend="touchEndElement">
    <view class="user-info">
      <image class="user-avatar" src="{{item[0].user.avatar}}"></image>
      <label class="user-name">{{item[0].user.name}}</label>
    </view>
    <view class="nav-icon">
    <label class="nav-btn-buy">去结算</label>
     <image src="../../images/arrow.png" mode="aspectFit" ></image>

      
    </view>

  
  </view>

  <view id="body-{{j}}" class="entry-body {{               activeHoverIndex=='body-'+j?'show-body-wrap': '' }}" bindtouchstart="touchStartElement" bindtouchmove="touchMoveElement" bindtouchend="touchEndElement">
    <view class="show-body"></view>
    <view class="book-list">
      <view class="entry-book" wx:for="{{item}}" wx:for-index="k" wx:key="id" >
        <image mode="aspectFit"  src="{{item.image_list[0]}}"></image>
        <label class="book-price">100书币</label>
      </view>

    </view>
    <view class="nav-cart">
      <view class=" transform-bg">
       </view>
      <view class=" nav-cart-label">
      共{{item.length}}本
      </view>
         <image  class="cart-item-list" src="../../images/arrow.png" mode="aspectFit" ></image>

    </view>
  
  
  </view>


</view>
